{
  "name": "cherry",
  "private": true,
  "description": "Cherry desktop studio — scaffold",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "echo 'Run dev:api and dev:web in two terminals (see README).'",
    "dev:api": "bash -lc 'set -a; [ -f .env ] && . ./.env; set +a; export MONGO_URI=\"${MONGO_URI:-mongodb://127.0.0.1:27017/cherry}\"; export CHERRY_CLOUDFLARED_BIN=\"${CHERRY_CLOUDFLARED_BIN:-$PWD/vendor/bin/cloudflared}\"; bash scripts/ensure-mongo.sh; go -C services/api run ./cmd/api'",
    "dev:web": "npm run dev --workspace cherry-web",
    "dev:desktop": "npm run start --workspace cherry-desktop",
    "build:web": "npm run build --workspace cherry-web",
    "start:web": "npm run start --workspace cherry-web",
    "lint": "npm run lint --workspaces --if-present"
  }
}
