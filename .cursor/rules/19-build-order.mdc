---
description: Yapım sırası — önce iskele, sonra tek LLM A; Colab ve iki model en sonda
globs:
  - docs/build-order.md
  - services/api/**
  - apps/**
alwaysApply: false
---

# Build order

- Follow [docs/build-order.md](docs/build-order.md). Pause checklist: [docs/remaining.md](docs/remaining.md).
- Next implementation slice is **6 — local activate + Maestro MCP**. Slices 1–5 are done. Do not stand up worker B or Colab until generated apps actually boot and Maestro can run or skip.
- OpenCode is the writer (`opencode run --dir`). Do not reimplement it. Missing CLI keeps the scaffold; do not fake an OpenCode write.
