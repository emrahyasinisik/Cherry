---
description: Maestro MCP — UI test, YAML flow, emulator, kırık düzeltme döngüsü
globs:
  - apps/desktop/**/maestro/**
  - "**/*.maestro.yaml"
  - "**/maestro/**"
alwaysApply: false
---

# Maestro MCP

- Wire Maestro through the desktop MCP host (`maestro mcp` stdio) and GraphQL `runMaestro` (`maestro test` on YAML).
- The Cherry installer vendors the CLI into `vendor/bin`. The customer does not install Maestro. PATH / `CHERRY_MAESTRO_BIN` is a developer fallback.
- Maestro is used by **whichever worker** (A or B) is free for that job. Every Maestro-related LLM call still goes through the GDPR layer. Do not reserve B for tests.
- Flows are YAML files owned by the generated project under `maestro/`. They ship with the customer files.
- If no emulator is available, still write flows and record SKIPPED; do not pretend a pass.
- Failure loop: inspect → fix code or flow → re-run. Bound the loop; do not infinite-heal.
- Read [docs/maestro.md](docs/maestro.md).
